# === Choisis UNE des 3 méthodes ci-dessous ===

# (A) Service pg (recommandé en local) — nécessite ~/.pg_service.conf
#PGSERVICE=ppea

# (B) DSN unique (override total) — pratique en CI/CD
#DATABASE_URL=service=ppea
#DATABASE_URL=postgresql://pea_user:***@127.0.0.1:5432/pea_db

# (C) Variables PG* (TCP explicite)
#PGHOST=127.0.0.1
#PGPORT=5432
#PGDATABASE=pea_db
#PGUSER=pea_user
#PGPASSWORD=***   # éviter, privilégier ~/.pgpass

# Options applicatives
APP_TZ=Europe/Paris
ACTIVE_MIN_CNT_1Y=200
VALID_WINDOW_DAYS=5
VALID_MIN_QUOTES_5D=2

# YFinance (exemples)
YF_SLEEP_SECS=2.0
YF_MAX_RETRIES=3
YF_TIMEOUT_SECS=20
